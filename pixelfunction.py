import numpy as np
from read_image import *

img = read_image.image
p = []

class PixelFunct(object):
    def __init__(self)
        for i in img:
            if i == 1:
                p += i
            else:
                continue

        for i in range(p):
            if i != range(p)[-1]:
                if p[i] == p[i+1]
                #...
            else:
                if p[i] == p[0]:
                    #...
